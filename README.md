This repository is a weather app, that can forecast the weather of a specified location, given that the location exist or is recognized by the backend servers.
The language used is Dart and the framework used is Flutter. 
Hive is used for as the technology for the local storage.
